module.exports = {
  name: "emojiCreate",
  /**
   *
   * @param {import("discord.js").Emoji} emoji
   * @param {import("../../Structures/bot")} client
   */
  async execute(emoji, client) {
    console.log(emoji);
  },
};
